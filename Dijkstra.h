#ifndef DIJKSTRA
#define DIJKSTRA
#include "Walls.h"
#include "Matrix.h"

//calculating the minimum number of doors
int get_result(int ** matrix, size_t size, size_t amount_of_outer_walls);

#endif